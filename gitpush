#!/bin/bash
git add . && git commit -m "Refactored components into ChatPane, SidePane, and MsgPane, each of which renders other components based on prop state (based on user entrance status)." && git push -u origin main